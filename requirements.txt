# Local CPU bootstrap only. This is NOT the final RGC lockfile.
#
# On RGC, first install the exact AMD PJRT/plugin/JAX/JAXLIB combination that
# matches the instance's ROCm release and GPU architecture. Then install this
# file and generate requirements/rgc.lock from the verified environment.
# See requirements/README.md. Never use this file as benchmark evidence.

-r requirements/base.in

# Container status

No Dockerfile is committed before Gate G0 because the RGC base image and digest
are not verified. After G0, pin the verified base by digest and keep direct RGC
installation instructions in `docs/REPRODUCTION.md` as a second path.

# Claude Code instructions

Read and follow `docs/PROJECT_RULES.md` before editing this repository. Treat it
as the canonical repository rule set. Then read `docs/DEVELOPMENT.md` and the
relevant architecture/config files for the current task.

The Chinese planning documents may exist one directory above this checkout, but
the repository must remain understandable and buildable without them.
